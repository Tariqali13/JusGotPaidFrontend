// @flow
import React from 'react';
import ReferralPaid from '@/components/referral-paid';
import SecureTemplate from '@/layout/secure-template';

const ReferralPaidMain = () => {
  return (
    <SecureTemplate>
      <ReferralPaid />
    </SecureTemplate>
  );
};

export default ReferralPaidMain;
